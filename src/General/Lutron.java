package General;

import java.util.HashMap;

public class Lutron {

//	public static void main(String[] args) {
//		// TODO Auto-generated method stub
//
//	}

	
	
}


/*public class Rectangle
{
    private int height, width;
    private Color fillColor;
    private Point location;
    private Stack<HashMap<String, Object>> s;
   
    public Rectangle (int h, int w, Color c, Point p)
    {
        this.height = h;
        this.width = w;
        this.fillColor = c;
        this.location = p;
        this.s = new Stack();

    }

	public int getHeight() {
		return height;
	}

	public void setHeight(int height) {
		HashMap<String, Object> h = new HashMap();
		h.put("height", this.height);
		s.push(h);

		this.height = height;
		
	}

	public void undo () {
		
		if (!s.isEmpty()) {
			HashMap<String, Object> h = s.pop();
			Object o = h.get("height");
			if (o != null) {
				this.height = (int) o;
			}
			
			Object o = h.get("height");
			if (o != null) {
				this.height = (int) o;
			}
			
			Object o = h.get("height");
			if (o != null) {
				this.height = (int) o;
			}
			
			Object o = h.get("height");
			if (o != null) {
				this.height = (int) o;
			}
		}
	}
	
}


Rectangle rec = new Rectangle(100, 20, Color.red, new Point(200,400)); 
rec.setHeight(10); 
rec.setWidth(20);
rec.setHeight(30);
rec.setColor(Color.blue); 

 

rec.undo();
rec.undo();
 */